name: test_env_xrft
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - xarray
  - dask
  - numpy
  - numpy_groupies
  - pytest
  - pytest-cov
  - pytest-lazy-fixture
  - pip
  - black
  - cftime
  - setuptools
  - pip:
    - codecov
    - pytest-cov
    - pytest-xdist
    - setuptools-scm
    - toml
